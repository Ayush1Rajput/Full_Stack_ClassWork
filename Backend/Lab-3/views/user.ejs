<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="style.css" />
  </head>

  <body>
    <a href="user/new">Create new User</a>
    <div class="container">
      <% if (userData && Array.isArray(userData)) { %> <% for (let user of
      userData) { %>
      <div class="box">
        <p>Name: <%= user.Usersname %></p>
        <hr />
      </div>
      <% } %> <% } else { %>
      <p>No user data available.</p>
      <% } %>
    </div>
  </body>
</html>
